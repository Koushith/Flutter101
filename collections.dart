//Collections This includes list, set, map

//list in some programming language it is called as arrays

main() {
  //we can define list using List keyword or can ignore that.
  List names = ["koushith", "Ankitha", "Swaroop", "Praneeth"];
  print(names[0]);

  //List is mutable, we can change the value
  names[2] = "Preetham";
  print(names);
// we can mention what type of elements DT it can store. 
  List<String> names2 = ["kou", "Anki"];
  print(names2);
}
