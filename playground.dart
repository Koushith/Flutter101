import 'dart:core'; //every standard built in types are coming from dart:core library. it is optional

// can be used with void or not this will not return anything
main() {
  var FirstName =
      "koushith"; //Type inference: Automatically detects what type it is.
  String LastName = "Amin"; // Statically Defined
  print(FirstName + " " + LastName);
}
